---
title: Hello, world
description: A placeholder post — replace or delete this once real writing exists.
date: '2026-01-01'
tags: [meta]
---

This is a placeholder post. Every file in `content/writing/*.md` shows up automatically on the
[Writing](/writing) index and gets its own page at `/writing/<filename>`.

## Front matter

Each post needs `title`, `description`, and `date` in its front matter — those drive the index
listing and page `<title>`/meta description.

## Delete me

Once there's real writing, delete this file (or overwrite it) — nothing else references it.
